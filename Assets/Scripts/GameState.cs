public static class GameState
{
    public static bool HasKey = false;

    public static bool IsPaused = false;

    // In GameState.cs — add this line alongside IsPaused
    public static bool IsPlayerFrozen = false;
}