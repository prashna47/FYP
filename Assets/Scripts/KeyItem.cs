using UnityEngine;

// A simple marker class to identify key objects
public class KeyItem : MonoBehaviour
{
    // This script doesn't need to contain anything else
    // It's just a way to identify key objects
}
